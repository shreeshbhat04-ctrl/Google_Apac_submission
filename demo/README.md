<!--
This directory will hold runnable demo assets for the submission.
The demos should prove hybrid search, reranking, and agent-triggered actions.
-->

<!--
The `payload_examples/` directory contains example JSON requests that match the
current generalized SDK/server shape. These are useful for mock-mode local runs,
demo scripts, and README snippets.
-->
